/// Enum for all available Vuesax icon variants
enum VuesaxVariant {
  bold('bold'),
  outline('outline'),
  linear('linear'),
  bulk('bulk'),
  twotone('twotone'),
  broken('broken');

  const VuesaxVariant(this.value);
  final String value;
}
